const storage = {
  user: {},
  userStatus: {}, // Object that holds the current guess, round counts, guessed list
  highScore: [], // Object that holds the highest score of every user
};

module.exports = storage;
